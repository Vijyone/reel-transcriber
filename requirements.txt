yt-dlp>=2026.3.17     # Latest stable. YouTube's anti-bot changes break older versions every few weeks.
notion-client>=2.2.1
python-dotenv>=1.0.1
instaloader>=4.13.0   # Fallback to get Instagram view counts (yt-dlp doesn't expose them).

# Streamlit UI (`./run.sh`)
streamlit>=1.40

# Local transcription on Apple Silicon
mlx-whisper>=0.4.0
